name: Build postproc-trigger image

on:
  push:
    branches: [master]
    paths:
      - postproc-trigger/**
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Log in to ghcr.io
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ./postproc-trigger
          push: true
          tags: ghcr.io/56k-modem/postproc-trigger:latest
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
